BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[RecordInventory] ADD [status] NVARCHAR(1000) NOT NULL CONSTRAINT [RecordInventory_status_df] DEFAULT 'use';

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
