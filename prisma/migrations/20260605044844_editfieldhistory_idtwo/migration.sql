BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[LogNotControl] ALTER COLUMN [incomingId] INT NULL;
ALTER TABLE [dbo].[LogNotControl] ADD [historyId] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
