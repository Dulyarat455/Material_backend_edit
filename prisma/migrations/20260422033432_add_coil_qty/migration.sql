BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[TransactionStoreHistory] ADD [coil] INT,
[qty] INT;

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
